# VHT Co-Pilot Backend - Requirements
# Production-ready packages for Render deployment

# Django Core
Django==5.0.2
djangorestframework==3.14.0
django-cors-headers==4.3.1
djangorestframework-simplejwt==5.3.1

# AI/ML Stack
openai==1.12.0
groq==0.11.0
langchain==0.1.7
# langchain-openai==0.0.5  # Only needed for OpenAI LLM operations
                           # Commented out - requires tiktoken (Rust compiler dependency)
                           # Production uses GROQ AI via direct API, not LangChain OpenAI
# chromadb==0.4.22  # Only needed for local embeddings (USE_LOCAL_EMBEDDINGS=true)
                    # Commented out for production - requires Rust compiler
                    # Production uses GROQ AI, which doesn't need vector DB

# Speech Recognition (server-side processing only)
SpeechRecognition==3.10.1
pydub==0.25.1  # Audio format conversion (uses FFmpeg, not PyAudio)

# Geospatial
geopy==2.4.1

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.1
requests==2.31.0
python-multipart==0.0.9

# Production Server
gunicorn==21.2.0
psycopg2-binary==2.9.9  # PostgreSQL for production
whitenoise==6.6.0  # Static file serving
dj-database-url==2.1.0  # Database URL parser

# Development & Testing (optional - not needed for deployment)
# pytest==8.0.0
# pytest-django==4.8.0
# PyAudio==0.2.14  # Only for local microphone input, not needed on server
# twilio==9.0.0
# africastalking==1.2.5
